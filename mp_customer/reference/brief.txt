================================================================================
BRIEF: Refresh customer dari reference ke MP_CUSTOMER_NEW
================================================================================

TUJUAN
------
Data aktif di MP_CUSTOMER_NEW HANYA dari reference:
  - INSERT baris baru yang belum ada
  - UPDATE createdate + account_trading_term untuk data existing yang match reference (kolom lain tetap)
  - DELETE baris lama yang triple-nya tidak ada di reference


SUMBER & TARGET
---------------
Sumber : [POWERAPPS].[dbo].[MP_CUSTOMER_REFERENCE_05_06_2026]
Target : [POWERAPPS].[dbo].[MP_CUSTOMER_NEW]


ATURAN UTAMA
------------
1. INSERT (baris baru saja)
   - Jika kombinasi customer_code + distributor_name + customer_name belum ada → INSERT full row.
   - Jika ketiga kolom sudah sama persis → tidak insert ulang (kolom lain tidak di-overwrite).

2. UPDATE createdate + account_trading_term (semua existing yang match)
   - Untuk row di MP_CUSTOMER_NEW yang match reference (code + distributor_name + customer_name)
     → UPDATE createdate dari customer_createdate reference.
     → UPDATE account_trading_term dari reference.
   - updated_at ikut di-set GETDATE().
   - Kolom lain existing TIDAK di-update.

3. Penanda match (UNIK = code + distributor + customer_name)
   - customer_code (reference) = code (MP_CUSTOMER_NEW)
   - distributor_name (reference) = Distributors.Distributor dari distributor_id
   - customer_name (reference) = name (MP_CUSTOMER_NEW)

   Contoh BOLEH insert baru:
   - code A + distributor B + nama X (belum ada)
   - code A + distributor B + nama Y (code & distributor sama, nama beda)

   Contoh UPDATE createdate (sudah ada):
   - code A + distributor B + nama X sudah ada → createdate diperbaiki dari reference

4. DELETE (data di luar reference)
   - Row di MP_CUSTOMER_NEW yang TIDAK match reference (code + distributor + name)
     → dihapus.
   - Row yang match reference → tetap ada (kolom lengkap dari data lama dipertahankan).

5. Duplikat di file reference
   - Duplikat = customer_code + distributor_name + customer_name sama persis.
   - Hanya baris pertama yang dipakai.
   - Salah satu kolom beda → bukan duplikat.


MAPPING KOLOM
-------------
INSERT (baris baru):
  customer_code, customer_name, city, customer_createdate, account_trading_term
  shop_type, shopper_type
  + lookup: distributor_id, account_id, regency_id
  (is_va tidak ada di reference → tetap NULL di MP_CUSTOMER_NEW)

UPDATE (existing only):
  customer_createdate   → createdate
  account_trading_term    → account_trading_term


MAPPING RELASI (lookup, untuk INSERT)
-------------------------------------
distributor_name → Distributors.Distributor → distributor_id
account_name     → Accounts.account         → account_id
regency_name     → Regencies.kota           → regency_id

Tidak ketemu → ID = NULL (tetap insert).


CATATAN TIPE DATA
-----------------
customer_createdate (reference) = DATE
  → MP_CUSTOMER_NEW.createdate (nvarchar) = teks DD/MM/YYYY
  → CONVERT(NVARCHAR(10), TRY_CAST(customer_createdate AS DATE), 103)

shop_type, shopper_type → NVARCHAR(255)
is_va                   → tidak diisi dari reference (NULL)

INSERT baru:
  status = 'active', created_at = GETDATE(), updated_at = GETDATE()

UPDATE existing:
  createdate, account_trading_term, updated_at yang berubah


RINGKASAN ALUR
--------------
1. Ambil + dedupe reference (customer_code + distributor_name + customer_name)
2. UPDATE createdate + account_trading_term semua existing yang match
3. INSERT baris baru yang belum ada
4. DELETE semua row yang triple-nya tidak ada di reference
5. Log insert + lookup miss


SCRIPT SQL (folder reference)
-----------------------------
1. SYNC_INSERT_MP_CUSTOMER_FROM_REFERENCE.sql
2. REPORT_CUSTOMERS_INSERTED_NEW.sql
3. REPORT_CUSTOMERS_LOOKUP_NOT_FOUND.sql

Log:
- MP_CUSTOMER_REF_SYNC_INSERT_LOG
- MP_CUSTOMER_REF_SYNC_LOOKUP_MISS_LOG

================================================================================
